# قاعدة البيانات
# للعمل المحلي (SQLite):
DATABASE_URL="file:./dev.db"
# للإنتاج (PostgreSQL / Supabase):
# DATABASE_URL="postgresql://postgres:password@db.xxxxx.supabase.co:5432/postgres"

# NextAuth
NEXTAUTH_SECRET="your-super-secret-key-change-in-production"
NEXTAUTH_URL="http://localhost:3000"

# Supabase (للإنتاج)
# NEXT_PUBLIC_SUPABASE_URL="https://xxxxx.supabase.co"
# NEXT_PUBLIC_SUPABASE_ANON_KEY="your-anon-key"

# البريد (Resend)
# RESEND_API_KEY="re_xxxxxxxx"
# EMAIL_FROM="ClassFlow <onboarding@resend.dev>"

# WhatsApp — إشعارات الأولياء عبر واتساب
# WHATSAPP_PROVIDER="ultramsg"      # أو "wati" أو "generic"
# WHATSAPP_API_URL="https://api.ultramsg.com"  # رابط API
# WHATSAPP_API_TOKEN="your-token"              # مفتاح API
# WHATSAPP_INSTANCE_ID="instance123"           # معرف الجلسة (لـ UltraMsg فقط)